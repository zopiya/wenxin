/* ──────────────────────────────────────────────
 * Custom JavaScript — Wenxin Theme
 * ──────────────────────────────────────────────
 *
 * This file is loaded after all theme scripts (Lenis, PJAX).
 * Use defer-friendly code — DOM is ready when this runs.
 *
 * ⚠ PJAX Re-initialization:
 *   This theme uses PJAX for partial page navigation.
 *   DOM state does NOT survive page transitions.
 *   If you attach event listeners or initialize libraries,
 *   re-run them inside the PJAX afterPageLoad callback:
 *
 *   document.addEventListener("pjax:afterPageLoad", function () {
 *     // Re-initialize your code here
 *   });
 *
 *   PJAX is disabled on mobile (max-width: 768px).
 * ────────────────────────────────────────────── */
